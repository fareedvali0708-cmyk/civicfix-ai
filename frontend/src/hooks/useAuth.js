/**
 * useAuth hook — thin re-export for ergonomic imports.
 *
 * Usage:
 *   import { useAuth } from '../hooks/useAuth';
 *   const { user, loading, signInWithGoogle, signOut } = useAuth();
 */
export { useAuth } from '../context/AuthContext.jsx';
