/**
 * Notification Service
 *
 * Responsibilities (to be implemented):
 * - Send status update notifications to citizens
 * - Notify officers of new assignments
 * - Support email and/or push notifications
 */

export async function notifyCitizen(_userId, _message) {
  // TODO: implement in a future step
  console.log(`[notificationService] Stub: would notify citizen ${_userId}`);
}

export async function notifyOfficer(_officerId, _message) {
  // TODO: implement in a future step
  console.log(`[notificationService] Stub: would notify officer ${_officerId}`);
}
