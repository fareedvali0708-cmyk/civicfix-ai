/**
 * Monitoring Job
 *
 * Responsibilities (to be implemented):
 * - Run on a scheduled interval (e.g., every 5 minutes via cron or setInterval)
 * - Query all open issues that may be breaching SLA
 * - Invoke Monitoring Agent for each stalled issue
 */

export function startMonitoringJob() {
  // TODO: implement in a future step
  console.log('[monitoringJob] Stub: monitoring job not yet started');
}
